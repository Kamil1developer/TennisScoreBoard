package dao;
import entity.Match;

import java.util.List;
import java.util.Optional;

public interface MatchDao {

    // В Java по умолчанию все методы интерфейса являются public, поэтому можно не указывать этот модификатор явно.

    // Можно назвать стандартно — 'save'
    public void insert(Match match);

    // Метод поиска по ID должен возвращать Optional<Match>. Но его функциональность не используется в проекте, поэтому стоит его удалить.
    public void findById();

    // Optional используется когда нужно аккуратно обработать возвращение пустого значения.
        // В случае с получением списка матчей пустым значением будет пустой объект List<Match>.
        // Его не нужно заворачивать в Optional.
    // TODO: В текущей сигнатуре метод подразумевает возврат абсолютно всех сущностей из таблицы базы данных одним запросом.
        // Это опасный подход с точки зрения производительности и потребления памяти.
        // Если таблица вырастет до значительных размеров (тысячи или миллионы записей), вызов этого метода
        // почти гарантированно приведёт к деградации производительности. Загрузка огромного объёма данных из БД
        // будет чрезвычайно медленной, создавая колоссальную нагрузку и на приложение, и на сервер БД.
        // Что в конечном итоге вызовет `OutOfMemoryError` — попытка загрузить все записи в память приложения приведёт к переполнению кучи (heap).
        // Метод должен принимать параметры `offset` (смещение) и `limit` (количество записей на странице).
    public Optional<List<Match>> findAll();

    // TODO: Согласно ТЗ, для страницы `/matches` необходим следующий функционал:
    /*
      - Постраничный вывод списка сыгранных матчей.
      - Поиск матчей по имени игрока.
     Для реализации этих требований DAO-интерфейс для матчей должен содержать методы, которые позволяют это сделать.
     Сейчас полностью соответствует необходимому функционалу только метод void insert(Match match).
     Стоит добавить в интерфейс и другие методы, необходимые для реализации бизнес-требований:
    
     Находит матчи с участием игрока с заданным именем (с пагинацией).
     @param playerName имя игрока для поиска.
     @param offset смещение (количество пропускаемых записей).
     @param limit максимальное количество записей на странице.
     @return список найденных матчей.
     List<Match> findByPlayerName(String playerName, int offset, int limit);

     Возвращает общее количество матчей, в которых участвовал игрок.
     @param playerName имя игрока.
     @return общее количество матчей.
     long countByPlayerName(String playerName);
    
     Находит все матчи с пагинацией.
     @param offset смещение.
     @param limit лимит.
     @return список матчей на странице.
     List<Match> findAll(int offset, int limit);
    
     Возвращает общее количество всех матчей в базе.
     @return общее количество матчей.
     long count();
    
     */
}
